import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.catedral.perfumes',
  appName: 'Catedral Perfumes',
  webDir: 'dist/catedral-perfumes/browser', 
  server: {
  },
};
export default config;